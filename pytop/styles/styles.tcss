MeterHeader {
    height: auto;
    margin: 1;
}

MeterHeader .meter-col {
    height: auto;
}

MeterHeader .meter-row {
    height: 1;
}

Main {
    overflow-y: hidden;
}

Main ProcessTable {
    height: 1fr;
    width: auto;
    overflow-x: hidden;
}
